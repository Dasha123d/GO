# Пример: HTTP-клиент (GET/POST)

Файл: `http-client.go`

## Назначение
Показывает использование базовых функций `http.Get` и создание кастомного клиента для POST-запроса.

## Запуск
```bash
go run http-client.go
```
Для работы требует запущенный `simple-server.go`.