# Пример: server-streaming клиент

Файл: `server-streaming-client.go`

## Назначение
Вызывает серверный стриминг `StreamData`, получает поток сообщений и выводит их.

## Запуск
```bash
go run server-streaming-client.go
```
Выведет пять сообщений с индексами 0-4.