# File Upload

Пример загрузки файла на сервер.

## Описание
Сервер принимает файл через форму (multipart/form-data), открывает поток чтения и сохраняет файл на диск в папку `uploads/`.

## Как проверить
```bash
curl -X POST -F "file=@/путь/к/вашему/файлу.jpg" http://localhost:8080/upload